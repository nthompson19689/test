import { MirrorLogClient } from '../../../components/white-room/MirrorLogClient';

export const metadata = {
  title: 'Mirror Log | WHITE ROOM',
  description: 'What I think I have noticed.',
};

export default function MirrorLogPage() {
  return <MirrorLogClient />;
}
