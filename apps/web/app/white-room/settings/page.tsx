import { SettingsClient } from '../../../components/white-room/SettingsClient';

export const metadata = {
  title: 'Settings | WHITE ROOM',
  description: 'Configure WHITE ROOM.',
};

export default function SettingsPage() {
  return <SettingsClient />;
}
